{
  "name": "rs-code-mcp",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun index.ts",
    "dev": "bun --watch index.ts",
    "typecheck": "bunx tsc -p tsconfig.json",
    "test": "bun test",
    "test:coverage": "bun test --coverage",
    "build:bin": "bun build --compile index.ts --outfile dist/rs-code-mcp"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.25.1",
    "zod": "^4.3.4"
  }
}
